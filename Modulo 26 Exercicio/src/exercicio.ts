

//função que faz a multiplicação

const multiply = (n1: number, n2: number): number => n1 * n2;

console.log(multiply(5,5))

// função que faz a saudação

const greeting = (nome:string): string => `Olá ${nome}`

console.log(greeting("Leonardo"));